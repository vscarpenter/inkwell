name: generated-file drift + contrast check

on:
  pull_request:
    paths:
      - 'inkwell-tokens.css'
      - 'tokens.json'
      - 'variants/*.css'
      - 'CHANGELOG.md'
      - 'examples/changelog.html'
      - 'scripts/build-tokens-json.mjs'
      - 'scripts/check-contrast.mjs'
      - 'scripts/build-changelog-html.mjs'
      - '.github/workflows/tokens-check.yml'
  push:
    branches: [main]
    paths:
      - 'inkwell-tokens.css'
      - 'tokens.json'
      - 'variants/*.css'
      - 'CHANGELOG.md'
      - 'examples/changelog.html'
      - 'scripts/build-tokens-json.mjs'
      - 'scripts/check-contrast.mjs'
      - 'scripts/build-changelog-html.mjs'
      - '.github/workflows/tokens-check.yml'

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '20'
      - name: Verify tokens.json is up to date
        run: node scripts/build-tokens-json.mjs --check
      - name: Verify WCAG contrast across palettes
        run: node scripts/check-contrast.mjs
      - name: Verify examples/changelog.html is up to date
        run: node scripts/build-changelog-html.mjs --check
