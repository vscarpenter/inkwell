name: Deploy examples to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'examples/**'
      - 'tokens.css'
      - 'inkwell.css'
      - 'inkwell-tokens.css'
      - 'inkwell-components.css'
      - 'inkwell-theme.css'
      - 'variants/clay.css'
      - 'variants/sage.css'
      - 'variants/burgundy.css'
      - 'variants/compare.html'
      - 'preview.html'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Sync canonical CSS + showcase HTML into examples/
        run: |
          cp tokens.css inkwell.css inkwell-tokens.css inkwell-components.css inkwell-theme.css examples/
          cp preview.html examples/
          mkdir -p examples/variants
          cp variants/clay.css variants/sage.css variants/burgundy.css variants/compare.html examples/variants/

      - name: Upload examples/ as Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./examples

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
